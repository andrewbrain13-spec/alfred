{
  "//": "Playwright MCP for Alfred's Prism approval. HEADLESS + loads the saved login session from prism_state.json (produced by automations/prism/login.py). Because the session is pre-loaded, the approval agent never logs in and never handles the password. Requires Node.js. If the state file path differs on your machine, update --storage-state.",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--headless", "--isolated", "--storage-state", "C:\\Alfred\\prism_state.json"]
    }
  }
}
