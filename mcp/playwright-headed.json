{
  "//": "Playwright MCP with a VISIBLE browser window, loading the saved session from prism_state.json. Use for debugging the approval flow so you can watch it. Same session as the headless config.",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--isolated", "--storage-state", "C:\\Alfred\\prism_state.json"]
    }
  }
}
