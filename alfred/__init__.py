"""Alfred: an email-triggered local workflow engine.

One always-on process watches a mailbox and, for each new message, runs the
workflows whose triggers match. Triggers and workflows are declared in a
YAML rules file so new automations need no code changes.
"""

__version__ = "0.1.0"
