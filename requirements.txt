# Core dependencies
PyYAML>=6.0
requests>=2.31

# IMAP source (default). Remove if you only use the Graph source.
imapclient>=3.0

# Graph source + automations (Microsoft 365). Required for source type: graph
# and the Prism/Piper/FollowUpThen automations that use alfred.graph.
msal>=1.28

# Prism MFA: generate one-time passcodes on demand (automation #1, browser path).
pyotp>=2.9

# Prism login script (automations/prism/login.py). After installing, run:
#   python -m playwright install chromium
playwright>=1.44

# Geodude daily email (automations/geodude/send_daily.py): read the Word doc
# and convert it to the email body.
python-docx>=1.1
mammoth>=1.7
